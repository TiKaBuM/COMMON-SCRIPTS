if(document.URL.search(/tribalwars/) > 0){
	var player = parseInt(game_data.player.id); // Busca ID do Player
var IdPlayer = [764126];
	for (var e = 0; e < IdPlayer.length; e++) {
    if (player === IdPlayer[e]) {

var Recarregar = 150;

if(game_data.screen = 'main'){
                setTimeout(
                    function() {
                            if ($("#bot_check").length == 0) {
                               var menu='main';
                               var urlTW='https://'+ game_data.world +'.tribalwars.com.br/game.php?village='+ game_data.village.id +'&screen='+ menu;
                                window.location=urlTW;
                            }
                    }, Recarregar);
            }
	}
	}
}
